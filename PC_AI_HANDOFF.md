# تقرير تسليم مشروع الباء إلى ذكاء الكمبيوتر

تاريخ التقرير: 2 أغسطس 2026

## المطلوب من المحادثة الموجودة على الكمبيوتر

اقرأ هذا التقرير ثم افحص التغييرات غير الملتزم بها في Git. حافظ على عمل المستخدم الحالي، أصلح أي مشاكل متبقية، لمّع الواجهة، شغّل الاختبارات، ثم ابنِ نسخة Windows جديدة. لا تعمل `git reset` ولا تحذف التغييرات الحالية. لا تنفذ commit أو push إلا بعد موافقة المستخدم.

## حالة المستودع

جميع التغييرات محفوظة محليًا لكنها غير ملتزم بها وغير مرفوعة إلى GitHub.

الملفات المعدلة أو الجديدة:

- `.gitignore`
- `AlBaaIDE.spec`
- `arabicpy/ai.py`
- `arabicpy/ai_server.py`
- `arabicpy/ide.py`
- `arabicpy/embedded_ai.py` (جديد)
- `arabicpy/rag.py` (جديد)
- `build_exe.ps1`
- `prepare_embedded_ai.ps1` (جديد)
- `pyproject.toml`
- `tests/test_embedded_ai.py` (جديد)
- `tests/test_rag.py` (جديد)

آخر تحقق:

- تشغيل واجهة الباء كاملة في وضع Qt offscreen نجح وظهر `IDE_STARTUP_OK`.
- جميع الاختبارات نجحت: `55 passed`.
- `git diff --check` لا يعرض أخطاء، فقط تحذيرات LF/CRLF.

## الميزات التي أضيفت

### الذكاء المحلي المضمّن

- اختيار نموذجين فقط:
  - `qwen3:1.7b` للابتوب، حجمه قرابة 2.2 GB.
  - `qwen3:8b` للكمبيوتر RTX 4070، حجمه قرابة 5.1 GB.
- الاختيار محفوظ لكل جهاز عبر QSettings بالمفتاح `ai_model`.
- Ollama يعمل إن كان مثبتًا، وإلا يستخدم البرنامج `llama.cpp` المضمّن.
- محرك `llama-server` يستمع محليًا فقط على `127.0.0.1:11435`.
- `prepare_embedded_ai.ps1` ينزّل نسخة Windows الرسمية من llama.cpp إلى `vendor/llama.cpp`.
- PyInstaller يضم المحرك تحت `ai-engine`.

### تنزيل النماذج

- شريط تقدم بالنسبة المئوية وGB.
- زر إيقاف/متابعة.
- الاستكمال بعد إغلاق البرنامج باستخدام ملف `.gguf.part`.
- دعم HTTP Range والتحقق من عدم تجاهل الخادم للاستكمال.
- تم تعطيل HTTP/2 للتنزيل بسبب ظهور `HTTP/2 protocol error` بعد 100% في Qt.
- إذا اكتملت البايتات فعلًا، يتحقق البرنامج من الحجم وترويسة `GGUF` ثم يعتمد الملف.
- يتعامل مع استجابة HTTP 416 عندما يكون ملف `.part` مكتملًا بالفعل.
- النماذج تحفظ في `%LOCALAPPDATA%\AlBaa\models`.

ملاحظة مهمة: الرسالة `تعذر تشغيل محرك الذكاء المضمّن` ما زالت عامة. يُفضّل التقاط خرج `llama-server` من QProcess وعرض آخر سطور الخطأ للمستخدم. الصورة التي ظهرت فيها المشكلة كانت تستخدم `qwen3:8b` على اللابتوب بدل `qwen3:1.7b`.

### RAG والمستندات

- قاعدة معرفة مدمجة للغة الباء مع استرجاع نصي خفيف شبيه BM25.
- RAG يدخل في رسائل مساعد IDE و`arabicpy.ai` وخادم الهاتف.
- زر `مستندات RAG` يستورد:
  - TXT, Markdown, APY, Python, JSON, CSV, PDF, DOCX.
- حد الملف 25 MB.
- المستندات تحفظ في `%LOCALAPPDATA%\AlBaa\rag_documents`.
- النص المستخرج يخزن في ملف `.rag` لتجنب إعادة الاستخراج.
- PDF النصي يستخدم pypdf.
- PDF المصوّر يستخدم PDFium + PaddleOCR PP-OCRv5 عربي/إنجليزي.
- تم تعطيل MKLDNN في PaddleOCR بسبب خطأ oneDNN على معالج اللابتوب.
- OCR الحقيقي اختُبر بنجاح من المصدر.

ملاحظة: OCR يعمل بشكل متزامن وقد يجمّد الواجهة أثناء مستند كبير. الأفضل نقله إلى QThread. نماذج PaddleOCR غير مضمّنة في dist وستنزل أول مرة على الجهاز الجديد بدون شريط تقدم مخصص.

### تعديلات الواجهة

- أيقونة العنوان وأيقونة مساعد الباء موحدتان كمستطيل صغير بحواف دائرية.
- أيقونة الإعدادات أصبحت ترسًا مرسومًا بدل رمز يشبه الشمس.
- أزرار نافذة Windows (تصغير/تكبير/إغلاق) مرسومة هندسيًا وليست أحرف خط.
- رمز الاستعادة صغير ويظهر منه المربع الخلفي من الأعلى واليمين فقط.
- النقر المزدوج على شريط العنوان يكبّر/يعيد النافذة.
- أزرار تراجع وإعادة في شريط الأدوات.
- الاختصارات:
  - Undo: Ctrl+Z
  - Redo: Ctrl+Y أو Ctrl+Shift+Z
- حالة الأزرار تتغير حسب تاريخ التبويب النشط.
- تمت تجربة قائمة نقر يمين بأسلوب Windows ثم أزيلت بالكامل بطلب المستخدم. القائمة الأصلية عادت.
- نافذة البحث القديمة QInputDialog أزيلت واستبدلت بشريط بحث مدمج داخل المحرر:
  - Enter للنتيجة التالية.
  - Escape للإغلاق.
  - يدعم الداكن والفاتح.
- حدث خطأ مؤقت لأن `FindInput` قُطع داخل `CodeEditor`، ثم أُصلح. تحقق تشغيل الواجهة الكامل نجح بعد الإصلاح.

### الحفظ التلقائي

- لكل تبويب مؤقت مستقل مدته ثانية بعد توقف الكتابة.
- يعمل فقط بعد حفظ الملف وتحديد اسمه أول مرة.
- يستخدم ملف `.autosave.tmp` ثم `os.replace` لحفظ ذري.
- يظهر مؤشر حالة أسفل البرنامج.
- الحفظ اليدوي يوقف المؤقت المتبقي ويستمر طبيعيًا.

## البناء والحجم

آخر نسخة مبنية موجودة في `dist\AlBaa` لكنها قديمة ولا تحتوي أحدث تعديلات الواجهة والحفظ والبحث.

- تاريخ آخر exe: 2 أغسطس 2026، 2:48 م.
- حجم `AlBaa.exe`: قرابة 42.79 MB.
- حجم مجلد التطبيق الكامل: قرابة 646 MB (677,408,584 بايت، 1138 ملفًا).
- يجب توزيع مجلد `dist\AlBaa` كاملًا، وليس exe وحده.
- السبب الأكبر للحجم هو PaddlePaddle/PaddleOCR، إضافة إلى Qt وPDFium وPython وllama.cpp.
- النماذج 2.2/5.1 GB ليست ضمن حجم 646 MB.

## قرار الإضافات المطلوب لاحقًا

المستخدم يريد تصغير البرنامج بتحويل الميزات الثقيلة إلى Extensions. الأولوية المقترحة:

1. OCR Extension (PaddlePaddle/PaddleOCR/PDFium).
2. Local AI Engine Extension (llama.cpp؛ النماذج تبقى تنزيلًا منفصلًا).
3. Android/APK tools.
4. GitHub integration.

لم يتم تنفيذ نظام Extensions حتى الآن.

## بناء المنصات — طلب جديد غير منفذ

المستخدم طلب إزالة زر `حزم المنصات` بصورته الحالية لأنه يصدّر مشروع Tauri للمطور فقط، واستبداله بزر واحد يبني وينزّل نتائج جاهزة:

- Windows EXE/installer
- Linux package
- macOS DMG
- Browser ZIP/static site

يجب أن يتم البناء عبر GitHub Actions لأن Windows لا يبني macOS محليًا. إعداد GitHub مسموح أن يكون مرة واحدة، وبعدها يريد المستخدم تجربة one-click. زر APK يبقى منفصلًا. هذا التغيير لم يُنفّذ بعد؛ الزر الحالي و`export_cross_platform` ما زالا موجودين. يوجد أساس جيد في `arabicpy/tauri_export.py`: workflow باسم `build-all.yml` يبني Web وWindows وLinux وmacOS، لكنه حاليًا يضيف Android وiOS أيضًا ويصدّر المصادر فقط من واجهة IDE.

## أولويات العمل على الكمبيوتر

1. شغّل البرنامج من المصدر وتحقق بصريًا من الداكن والفاتح، خصوصًا شريط البحث وأيقونات النافذة.
2. اختبر تنزيل وتشغيل `qwen3:8b` على RTX 4070 فعليًا، واعرض سجل llama-server عند الفشل.
3. اختبر RAG مع PDF نصي وPDF مصوّر من داخل الواجهة.
4. راجع الحفظ التلقائي مع عدة تبويبات وملفات خارج المشروع.
5. نفّذ زر بناء المنصات one-click عبر GitHub Actions، مع تنزيل كل artifacts إلى مجلد نتائج واضح.
6. افصل OCR كإضافة اختيارية لتقليل حجم النسخة الأساسية.
7. شغّل الاختبارات وابدأ تشغيل GUI فعليًا، ثم أعد بناء `dist\AlBaa`.
8. اعرض على المستخدم النتائج والحجم الجديد قبل commit أو push.

## أمر مقترح للمستخدم في المحادثة القديمة

> اقرأ ملف `PC_AI_HANDOFF.md` كاملًا، ثم افحص التغييرات الحالية بدون حذفها. أكمل الأولويات المذكورة، اختبر البرنامج فعليًا على RTX 4070، ولمّع المشروع. لا تعمل commit أو push قبل أن تسألني.
